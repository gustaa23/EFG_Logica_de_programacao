programa {
  funcao inicio() {
    real salario, porcentagem
    escreva("Qual seu salario? ")
    leia(salario)
    porcentagem = salario * 15 / 100
    escreva("Seu salário é ", salario + porcentagem)
  }
}
